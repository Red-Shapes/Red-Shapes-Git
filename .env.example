# Example environment variables
API_URL=https://red-shapes-git.onrender.com/api
# JWT secret for signing tokens. In production, set a strong secret as an environment variable.
JWT_SECRET=change-me-to-a-secure-secret
